Your Computer on the Network
============================

Basic Networking
----------------

Computers use IP addresses (like street adress) to communicate.

DNS (Domain Names Services) translates host/website names to IP addresses.

Networking in Linux
--------------------

View IP addresses on machine with `ip a`.

View the default routes (gateways) with `ip r`
